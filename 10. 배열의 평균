#include <string>
#include <vector>
#include <iostream>

using namespace std;

double solution(vector<int> numbers) {
    double answer = 0;
    for(int n : numbers){
        answer += n;
    }
    answer = answer/numbers.size();
    
    return answer;
}
