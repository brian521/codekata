#include <string>
#include <vector>

using namespace std;

int solution(vector<int> numbers) {
    int answer = -1;
    int hap = 45;
    for(int n : numbers){
        hap -= n;
    }
    answer = hap;
    return answer;
}
