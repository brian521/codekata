#include <iostream>
#include <string>
#include <vector>

using namespace std;

int solution(int num1, int num2) {
    int answer = 0;
    answer = num1 - num2;
    return answer;
}

int main() {
    int input1, input2;
    cin >> input1 >> input2;
    cout << solution(input1, input2);
}
