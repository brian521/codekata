#include <string>
#include <vector>
#include <iostream>
#include <cmath>

using namespace std;

long long solution(long long n) {
    long long answer = 0;
    double x = sqrt(n);
    if(x == int(x)) {
        answer = pow(x+1, 2);
    }
    else {
        answer = -1;
    }
    return answer;
}
